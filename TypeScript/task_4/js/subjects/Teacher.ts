// Ceci nous permet de créer un namespace où on pourra entrer
// Le nom complet de l'enseignant.
namespace Subjects {
    export interface Teacher {
        firstName: string;
        lastName: string;
    }
}
